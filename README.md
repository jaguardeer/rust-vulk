Minimal example of calling a C library from Rust. Includes equivalent program in C and Rust.
Build the Rust version with `./build-run-rust-version.ps1`
Build the C version with `./build-run-c-version.ps1`
